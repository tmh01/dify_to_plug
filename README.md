# Dify Chatflow 浏览器扩展说明

## 项目概述

本项目是一个基于 Chromium Manifest V3 的轻量级浏览器扩展，用于在 Chrome、Edge 等 Chromium 内核浏览器中，通过工具栏弹窗访问已发布的 Dify Chatflow/Chatbot 应用。

扩展的核心实现方式是：在浏览器扩展的 `popup.html` 页面中嵌入指定的 Dify 应用链接，使用户无需单独打开网页即可访问对应的 AI 应用。该扩展适用于个人学习、内部演示、课程项目展示或轻量级工具入口封装等场景。

## 功能范围

本扩展主要包含以下功能：

1. **浏览器弹窗入口**  
   通过浏览器扩展按钮打开一个固定尺寸的弹窗页面。

2. **Dify 应用嵌入**  
   在弹窗中使用 `iframe` 加载已发布的 Dify Chatflow/Chatbot 页面。

3. **基础交互权限声明**  
   在 `iframe` 中声明剪贴板读写、麦克风等权限，以支持 Dify 页面中的常见交互能力。

4. **页面嵌入兼容配置**  
   通过 `declarativeNetRequest` 对 `udify.app` 子框架请求的部分响应头进行处理，以提升 Dify 页面在扩展弹窗中加载的兼容性。

## 文件结构

本项目主要由以下文件组成：

```text
MyDifyPlugin/
├── manifest.json   # 浏览器扩展配置文件
├── popup.html      # 扩展弹窗页面
└── rules.json      # 网络请求响应头处理规则
```

其中：

- `manifest.json`：定义扩展名称、版本、弹窗入口、权限和网络规则资源。
- `popup.html`：定义扩展弹窗的页面结构，并通过 `iframe` 加载 Dify 应用。
- `rules.json`：定义针对 `udify.app` 子框架请求的响应头处理规则。

## 运行环境

使用本扩展前，需要准备以下环境：

1. Chrome、Edge 或其他支持 Manifest V3 的 Chromium 内核浏览器。
2. 一个已发布并可访问的 Dify Chatflow/Chatbot 应用链接。
3. 本地扩展文件夹，至少包含 `manifest.json`、`popup.html` 和 `rules.json`。

## 配置说明

### 1. 配置 Dify 应用链接

在 `popup.html` 中，将 `iframe` 的 `src` 修改为自己的 Dify 应用公开访问链接，例如：

```html
<iframe src="https://udify.app/chatbot/你的应用ID" allow="clipboard-read; clipboard-write; microphone"></iframe>
```

需要注意：

- 链接应来自 Dify 应用发布后的公开访问地址。
- 若 Dify 应用未发布，或访问权限受限，浏览器扩展中可能无法正常加载。
- 若使用的是私有部署的 Dify，需要将链接替换为对应部署环境中的应用地址。

### 2. 配置扩展基本信息

在 `manifest.json` 中，可以根据项目实际情况修改扩展名称、版本号和描述：

```json
{
  "manifest_version": 3,
  "name": "Dify Chatflow 浏览器扩展",
  "version": "1.0.0",
  "description": "用于在浏览器工具栏弹窗中访问指定 Dify Chatflow/Chatbot 应用的轻量级扩展。"
}
```

如果需要设置扩展图标，可以在项目目录中添加图标文件，并在 `manifest.json` 中补充 `icons` 配置。

### 3. 网络规则说明

本项目中的 `rules.json` 用于处理 `udify.app` 子框架请求中的部分响应头，以便 Dify 页面能够在浏览器扩展弹窗中嵌入显示。

该规则仅针对 `udify.app` 相关子框架请求生效。若部署环境、域名或浏览器安全策略发生变化，需要根据实际情况调整规则配置。

## 本地安装与测试

### Edge 浏览器

1. 在地址栏输入 `edge://extensions/` 并进入扩展管理页面。
2. 打开“开发人员模式”。
3. 点击“加载解压缩的扩展”。
4. 选择包含 `manifest.json` 的插件文件夹。
5. 在浏览器工具栏中固定该扩展。
6. 点击扩展图标，检查 Dify 应用是否能够正常加载。

### Chrome 浏览器

1. 在地址栏输入 `chrome://extensions/` 并进入扩展管理页面。
2. 打开“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择包含 `manifest.json` 的插件文件夹。
5. 在浏览器工具栏中固定该扩展。
6. 点击扩展图标，检查 Dify 应用是否能够正常加载。

## 打包与发布说明

如果需要将扩展提交到浏览器扩展商店，应将 `manifest.json`、`popup.html`、`rules.json` 等文件置于压缩包根目录中，再打包为 `.zip` 文件。

打包时应注意：

- 压缩包根目录下应直接包含 `manifest.json`，避免多嵌套一层文件夹。
- 不建议将浏览器自动生成的 `_metadata` 目录作为源码一并提交。
- 不应在扩展源码中写入私有 API Key、账号密码或其他敏感信息。
- 提交 Chrome Web Store 或 Microsoft Edge Add-ons 时，应根据平台要求填写隐私政策、权限说明和应用介绍。

## 使用限制与注意事项

1. **依赖 Dify 应用的可访问性**  
   扩展本身不提供 AI 推理能力，只负责加载指定的 Dify 应用页面。Dify 应用是否可用取决于 Dify 服务状态、应用发布状态和访问权限配置。

2. **受浏览器安全策略影响**  
   不同浏览器版本、Dify 页面策略或扩展商店审核规则可能影响 `iframe` 嵌入效果。若页面无法加载，需要检查控制台错误、域名配置和响应头规则。

3. **权限范围应保持最小化**  
   当前扩展仅围绕加载 Dify 页面所需能力进行配置。若后续增加更多浏览器能力，应明确说明新增权限的用途。

4. **发布前需进行合规检查**  
   若计划公开发布扩展，应确认其响应头处理方式、隐私声明、数据处理说明和权限申请符合目标平台的审核要求。

## 适用场景

本扩展适合以下场景：

- 将个人或团队的 Dify 应用封装为浏览器工具入口。
- 在课程项目、演示答辩或内部测试中快速展示 Dify Chatflow。
- 为常用的 Dify 工作流提供固定访问入口。
- 在不开发完整前端系统的情况下，实现轻量级浏览器端访问方式。

## 不适用场景

本扩展不适合以下场景：

- 需要复杂浏览器自动化能力的插件系统。
- 需要本地持久化大量用户数据的应用。
- 需要独立账户体系、权限管理或完整前端交互逻辑的生产级系统。
- 对浏览器扩展商店审核合规性有严格要求但尚未完成安全评估的公开发布场景。
